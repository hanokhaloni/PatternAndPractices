class car {
  constructor(bodyType, engine, tires, modelDate, red, green, blue) {
    //code
    //...

    // this.red = red; //used as number?
    // this.blue = blue; //used as number?
    // this.green = green; //used as number?
  }

  setColor(red, green, blue) {
    // this.red = red; //used as number?
    // this.blue = blue; //used as number?
    // this.green = green; //used as number?)
  }

  setEngine(fuelType, valveCount, meetEmission, liscenceNumber, isTurbo, fuelTipe, volume) {
      //....
  }
}
